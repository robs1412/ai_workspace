# OpenWrt 25.12.2 Custom Image Build Evaluation Plan

- Master Incident ID: `AI-INC-20260412-OPENWRT-LUCI-UPGRADE-01`
- Last Updated: 2026-04-13 20:04:03 CDT (Machine: Macmini.lan)
- Scope: prepare build/evaluation plan only; no custom image built in this pass
- Related OPS/Portal task: `366563`

## Approval Boundary

Robert approved continuing evaluation and preparing commands/checklists/artifact paths. The existing checklist requires separate approval before downloading ImageBuilder or building/requesting a custom image. Therefore this pass stops before the actual custom image build/request.

Exact approval needed to proceed with build-only local evaluation:

> Approve local custom OpenWrt 25.12.2 WRT3200ACM image build/request evaluation. This approval allows hydrating non-secret package metadata, downloading/verifying ImageBuilder or using the official Firmware Selector/custom builder, generating a local custom image and manifest, and saving local artifacts under `.private/router/openwrt-upgrade-20260414-1000/custom-image-evaluation/`. It does not allow copying anything to the router, running `sysupgrade -T`, flashing firmware, rebooting, reloading firewall/VPN, restarting StrongSwan/WireGuard, installing live-router packages, or printing secrets.

## Ready Inputs

- Candidate package input file: `project_hub/issues/2026-04-14-openwrt-25.12.2-custom-image-package-input.txt`.
- Existing project note: `project_hub/issues/2026-04-12-openwrt-luci-upgrade-assessment.md`.
- Existing checklist: `project_hub/issues/2026-04-14-openwrt-25.12.2-custom-image-evaluation-checklist.md`.
- Existing private staging root: `.private/router/openwrt-upgrade-20260414-1000`.
- Future custom-image artifact root: `.private/router/openwrt-upgrade-20260414-1000/custom-image-evaluation/`.

## Official Metadata Confirmed

- Target/profile: `mvebu/cortexa9`, `linksys_wrt3200acm`.
- Supported device includes `linksys,wrt3200acm`.
- Official sysupgrade image: `openwrt-25.12.2-mvebu-cortexa9-linksys_wrt3200acm-squashfs-sysupgrade.bin`.
- Official sysupgrade SHA256: `cf1ab7a2cafd6e317afc76cf1653be774474ba9ca3f74e6cdf5bd26118c4640c`.
- Official ImageBuilder tarball: `openwrt-imagebuilder-25.12.2-mvebu-cortexa9.Linux-x86_64.tar.zst`.
- Official ImageBuilder SHA256: `87fba1a44f6fa07660da4278bb4ef27e6aade813a7fde2cfe1dd81edcf09220f`.
- Official `profiles.json` SHA256: `11b18b826bc4c8e56188a5efe125a12c6275949f7bc1d24bba07e5e154caf620`.

## Package Availability Confirmed From Public Feeds

Checked OpenWrt `25.12.2` package feeds for `arm_cortex-a9_vfpv3-d16`.

- Base feed has `dnsmasq`, `firewall4`, `nftables-json`, `uhttpd`, `uhttpd-mod-ubus`, `rpcd`, and `wireguard-tools`.
- LuCI feed has `luci`, `luci-base`, `luci-app-firewall`, `luci-app-package-manager`, `luci-proto-wireguard`, `luci-app-uhttpd`, and `luci-app-strongswan-swanctl`.
- Packages feed has `rpcd-mod-wireguard` and the StrongSwan `6.0.3-r1` family, including `strongswan`, `strongswan-default`, `strongswan-swanctl`, `strongswan-pki`, `strongswan-mod-eap-mschapv2`, `strongswan-mod-vici`, `strongswan-mod-kernel-netlink`, `strongswan-mod-kdf`, crypto modules, X.509, and related modules.
- Target `sha256sums` has `kmod-wireguard`, `kmod-mwlwifi`, and `kmod-dsa-mv88e6xxx` under the `6.12.74` kmods directory.

## Local Metadata Gap

The private saved metadata files under `.private/router/openwrt-upgrade-20260414-1000` are currently Google Drive File Provider placeholders on this machine. They report logical sizes but `0` local blocks. `brctl` cannot hydrate them because this is not iCloud CloudDocs, and `fileproviderctl` on this system does not expose a materialize command.

Impact: the package input file is a strong candidate derived from the project note and public metadata, but it is not yet reconciled against the saved `custom-image-critical-package-set.txt` or full `opkg-list-installed-24.10.5.txt`.

Before a build, hydrate at least these non-secret files through Finder/Google Drive or another safe sync path:

- `.private/router/openwrt-upgrade-20260414-1000/metadata/custom-image-critical-package-set.txt`
- `.private/router/openwrt-upgrade-20260414-1000/metadata/opkg-list-installed-24.10.5.txt`
- `.private/router/openwrt-upgrade-20260414-1000/metadata/storage-partitions-bootenv.txt`
- `.private/router/openwrt-upgrade-20260414-1000/metadata/sysupgrade-T-25.12.2-output.txt`
- `.private/router/openwrt-upgrade-20260414-1000/official/profiles.json`
- `.private/router/openwrt-upgrade-20260414-1000/official/sha256sums`

Do not print secret-bearing backup contents.

## Build Evaluation Paths

### Option A: Official Firmware Selector / Custom Builder

Use this if speed and lower local tooling burden matter more than fully local reproducibility.

Inputs:

- Version: `25.12.2`
- Target: `mvebu/cortexa9`
- Profile: `linksys_wrt3200acm`
- Package list: reconciled non-secret package input list
- Do not upload secret config files

Required artifacts to save:

- package list submitted
- builder URL or request details without secrets
- generated image filename/size/SHA256
- generated manifest
- package-manifest comparison result

### Option B: Local ImageBuilder

Use this if reproducibility and local artifact traceability matter more than speed.

Future build-only commands after separate approval:

```sh
cd ".private/router/openwrt-upgrade-20260414-1000/custom-image-evaluation"
curl -fsSLO "https://downloads.openwrt.org/releases/25.12.2/targets/mvebu/cortexa9/openwrt-imagebuilder-25.12.2-mvebu-cortexa9.Linux-x86_64.tar.zst"
printf '%s  %s\n' "87fba1a44f6fa07660da4278bb4ef27e6aade813a7fde2cfe1dd81edcf09220f" "openwrt-imagebuilder-25.12.2-mvebu-cortexa9.Linux-x86_64.tar.zst" | shasum -a 256 -c -
tar --use-compress-program=unzstd -xf openwrt-imagebuilder-25.12.2-mvebu-cortexa9.Linux-x86_64.tar.zst
cd openwrt-imagebuilder-25.12.2-mvebu-cortexa9.Linux-x86_64
make info
make image PROFILE=linksys_wrt3200acm PACKAGES="$(tr '\n' ' ' < ../../reconciled-package-input.txt)"
```

Required artifacts to save:

- `reconciled-package-input.txt`
- ImageBuilder tarball SHA256 verification output
- `make info` output
- build command line with package list, without secrets
- generated `bin/targets/mvebu/cortexa9/*.manifest`
- generated `*-linksys_wrt3200acm-*-sysupgrade.bin`
- generated image SHA256 and size
- manifest comparison report

## Acceptance Criteria Before Router-Side Validation

Do not stage a custom image to the router until all criteria pass:

- Generated target/profile remains `mvebu/cortexa9` / `linksys_wrt3200acm`.
- Image type is sysupgrade, not factory.
- Manifest contains router access packages: `dropbear`, LuCI stack, `uhttpd`, `uhttpd-mod-ubus`, `rpcd`.
- Manifest contains firewall/DNS packages: `firewall4`, `nftables-json`, `dnsmasq`.
- Manifest contains WireGuard fallback packages: `kmod-wireguard`, `wireguard-tools`, `luci-proto-wireguard`, `rpcd-mod-wireguard`.
- Manifest contains StrongSwan/IKEv2 packages: `strongswan`, `strongswan-default`, `strongswan-swanctl`, `strongswan-pki`, EAP-MSCHAPv2, VICI, kernel-netlink, KDF, crypto, X.509, and current module families.
- Manifest contains WRT3200ACM device default packages from `profiles.json`.
- Generated image size/hash are recorded.
- Robert explicitly approves router-side validation-only staging for that exact image path and SHA256.

## Requires Separate Approval After Build

- Copying the custom image to `/tmp` on the router.
- Running validation-only `sysupgrade -T` for the custom image.
- Any actual flash.
- Any reboot or service reload/restart.
- Any live-router package installation.

## Tuesday Readiness State

Ready for Tuesday `2026-04-14 10:00 CDT` as an evaluation package, not as an upgrade package. The concrete blocker before custom image build is explicit build-only approval plus hydration/reconciliation of non-secret private package metadata.
